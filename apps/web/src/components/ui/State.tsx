export function LoadingState() { return <div className="animate-pulse rounded-xl border border-slate-200 bg-white p-8 text-sm text-slate-500">Loading authoritative recovery data…</div>; }
export function EmptyState({ title, detail }: { title: string; detail: string }) { return <div className="rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center"><h2 className="font-semibold text-slate-800">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm text-slate-500">{detail}</p></div>; }
export function ErrorState({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="rounded-xl border border-rose-200 bg-rose-50 p-5 text-rose-800"><p className="font-semibold">Could not load recovery data</p><p className="mt-1 text-sm">{message}</p><button onClick={retry} className="mt-3 rounded-md bg-white px-3 py-1.5 text-sm font-semibold ring-1 ring-rose-300">Try again</button></div>; }
